arch installation bootstrap scripts

to run:

    curl -sL https://waa.ai/arch_bootstrap | bash

will default to hostname "koto", use the following to use a different one:

    curl -sL https://waa.ai/arch_bootstrap | bash -s kirie
